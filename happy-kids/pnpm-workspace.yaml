# pnpm 11 требует явного решения по build-скриптам зависимостей.
# Для статического экспорта нативная сборка не нужна (next/image unoptimized),
# поэтому отключаем — это снимает ERR_PNPM_IGNORED_BUILDS в CI.
allowBuilds:
  sharp: false
  unrs-resolver: false
